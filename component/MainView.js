import { useState } from "react";
import { Text, TouchableOpacity, ScrollView, StyleSheet } from "react-native";
import { globalStyle } from "./GlobalStyle";

return (
    <Vliew style = {globalStyle.container}>
        <ScrollView>
            
        </ScrollView>
    </Vliew>
)